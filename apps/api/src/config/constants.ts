/** Platform commission: 7% of gross payment (company receives remainder). */
export const PLATFORM_FEE_RATE = 0.07;

/** Default seat lock TTL when env not set (minutes). */
export const DEFAULT_SEAT_LOCK_TTL_MINUTES = 15;
