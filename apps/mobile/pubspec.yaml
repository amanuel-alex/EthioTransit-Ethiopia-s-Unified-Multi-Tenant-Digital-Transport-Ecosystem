name: ethiotransit_mobile
description: EthioTransit passenger app — search, book, pay, ticket.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  dio: ^5.8.0
  flutter_secure_storage: ^9.2.4
  intl: ^0.20.2
  qr_flutter: ^4.1.0
  share_plus: ^10.1.4
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
